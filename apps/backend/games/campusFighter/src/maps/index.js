import {TMX} from "../tiled/index"
import gigantic from "./gigantic.json"
import small from "./small.json"

/**@type {Object<string, TMX.IMap>} */
export const List = Object.freeze({
    small,
    gigantic,
});